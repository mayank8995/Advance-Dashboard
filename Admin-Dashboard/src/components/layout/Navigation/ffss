// Layout.jsx
export default function Layout({ children }) {
  return (
    <div className="h-screen flex flex-col overflow-hidden bg-slate-100">

      {/* HEADER */}
      <header className="h-14 min-h-14 bg-white border-b border-slate-200 flex items-center px-5 gap-3 z-10">
        {/* logo, nav, user profile */}
      </header>

      {/* BODY */}
      <div className="flex flex-1 overflow-hidden">

        {/* SIDEBAR */}
        <aside className="w-52 min-w-52 bg-white border-r border-slate-200 flex flex-col overflow-y-auto p-3">
          {/* nav items */}
        </aside>

        {/* MAIN — only this scrolls */}
        <main className="flex-1 overflow-y-auto p-5">
          {children}
        </main>

      </div>

      {/* FOOTER */}
      <footer className="h-9 min-h-9 bg-white border-t border-slate-200 flex items-center px-5">
        {/* footer content */}
      </footer>

    </div>
  );
}