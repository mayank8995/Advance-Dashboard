function isPalindrome(str) {
  let i = 0; 
  let j = str.length - 1;
  while(i != j && i < j){
    if(str[i] !== str[j]) return false;
    i++;
    j--;
  }
  return true;
}
console.log(isPalindrome("malayalam"))

function findMax(arr) {
  let max = arr[0];
  let i = 1;
  while(i < arr.length){
    if(arr[i] > max){
          max = arr[i];
    }
    i++;
  }
  return max;
}
console.log(findMax([3,5,66,-1,0]))

function charCount(str) {
  let obj = {};
  for(let i = 0; i < str.length; i++){
    let char = str[i]; 
    obj[char] = (obj[char] || 0) + 1;
  }
  return obj;
}

console.log(charCount("banana"))

Input:
arr = [2, 7, 11, 15]
target = 9
Output:
[0, 1]

 function twoSum(arr, target) {
    let sum = arr[0];
    for(let i = 0 ; i < arr.length ; i++){
        for(let j = i+1 ; j < arr.length ; j++){
          if(arr[i] + arr[j] === target) return [i, j]
      }
    }
  return "target not found"
}
console.log(twoSum([2, 17, 7, 15], 9))

function twoSum(arr, target){
    let map = new Map();
    for(let i = 0; i < arr.length; i++){
        let complement = target - arr[i];
        if(map.has(complement)){
            return [map.get(complement),i]
        }else{
            map.set(arr[i],i)
        }
    }
    return "No sum exists";
}

console.log(twoSum([2, 17, 7, 15], 9))
arr = [0,1,0,0,0,0,5,6]
function moveZeroes(arr){
    let slow = 0;
    let fast = 0;
    while(fast < arr.length){
        if(arr[fast] !== 0){
                let temp = arr[slow];
                arr[slow] = arr[fast];
                arr[fast] = temp;
                slow++;
        }
        fast++;
        
    }
        return arr;
   }
console.log(moveZeroes([1,2,3,4,5,6]))

   pwwkew
function longestSubstring(str) {

    let set = new Set();
    let slow = 0;
    let maxLength = 0;
    for(let fast = 0; fast < str.length; fast++){

      while(set.has(str[fast])){
        set.delete(str[slow]);
        slow++;
      }
      set.add(str[fast]);
      maxLength = Math.max(maxLength, fast - slow + 1);
    }
  return maxLength
}

console.log(longestSubstring("aaaba"))

Return length of longest contiguous subarray containing at most 2 distinct numbers.
function LongestSubArray(arr){
   let slow = 0;
   let fast;
   let maxLength = 0;
   let map = new Map();
   for(fast = 0; fast<arr.length; fast++){
        map.has(arr[fast]) ?  map.set(arr[fast], map.get(arr[fast]) + 1) : map.set(arr[fast], 1);
        while(map.size > 2){
          map.set(arr[slow], map.get(arr[slow]) - 1);
          if(map.get(arr?.[slow]) === 0) map.delete(arr[slow]);
          slow++;
        }
        maxLength = Math.max(maxLength, fast - slow + 1)
      }
   return maxLength;
}
console.log(LongestSubArray([1, 2, 1, 3, 2, 2]))
console.log(LongestSubArray([1,2,3,9,9,1,2,1,2,3,4,5,7,8,9,5,8,9,9,9,9,9,9]))
console.log(LongestSubArray([1,2,1,2,1,2,1,2,1,2,3,4,5,7,8,9,9,9,9,9,9,9,9,9,9,9,9,9,9,9,9]))
console.log(LongestSubArray([1,1,1,2,2,3,3,3,2,2,2]))
console.log(LongestSubArray([1,1,1,1,1]))
console.log(LongestSubArray([1,2,3,3,4]))
console.log(LongestSubArray([1,2,1,2,3]))



Maximum Sum Subarray of Size K
function maxSumSubarray(arr,k){
    if(k > arr.length) return "No valid maxsum of subarray of size k exists"
    let slow = 0;
    let currentWindowSum = 0;
    let maxSum = Number.MIN_SAFE_INTEGER;
    for(let fast = 0; fast < arr.length; fast++){
        currentWindowSum += arr[fast];
        if(fast - slow + 1 === k){
            maxSum = Math.max(maxSum, currentWindowSum)
            currentWindowSum -= arr[slow];
            slow++;
        }
    }
    return maxSum;
}
console.log(maxSumSubarray([2, 1, 5, 1, 3, 2],3))
console.log(maxSumSubarray([2,2,2,2,2,2],3))
console.log(maxSumSubarray([2, 1, 5, 1, 3, 2,1,1,1,1,1,1,1,1,1],5))
console.log(maxSumSubarray([1,1,1,1,1,1,1,1,1],4))
console.log(maxSumSubarray([1,2,3,4,5,6,7,8,9],3))
console.log(maxSumSubarray([2,4,3,6,5,7,8],4))
console.log(maxSumSubarray([],4))
console.log(maxSumSubarray([-5, -2, -8],2))



first subarray of size K with sum 7
function fisrtsumSubArray(arr,k){
    if(k > arr.length) return "No valid subarray of size k with sum 7 exists"
    let slow = 0;
    let currentWindowSum = 0;
    let fast = 0;
    for(fast = 0; fast < arr.length; fast++){
        currentWindowSum += arr[fast];
        if(fast - slow + 1 === k){
            if(currentWindowSum === 7) return arr.slice(slow,fast+1);
            currentWindowSum -= arr[slow];
            slow++;
        }
    }
    return "No valid subarray of size k with sum 7 exists"
}

console.log(fisrtsumSubArray([2, 1, 5, 1, 3, 2],3))
console.log(fisrtsumSubArray([2,2,2,2,2,2],3))
console.log(fisrtsumSubArray([2, 1, 5, 1, 3, 2,1,1,1,1,1,1,1,1,1],7))
console.log(fisrtsumSubArray([1,1,1,1,1,1,1,1,1],4))
console.log(fisrtsumSubArray([1,2,3,4,5,6,7,8,9],3))
console.log(fisrtsumSubArray([2,4,3,6,5,7,8],4))
console.log(fisrtsumSubArray([],4))
console.log(fisrtsumSubArray([-5, -2, -8],2))

Return the length of the smallest contiguous subarray whose sum is greater than or equal to target
arr = [2,1,5,2,3,2]
target = 7
 

function smallestContiguousSubarrayLength(arr,target){
    let slow = 0;
    let minLength = Number.MAX_SAFE_INTEGER;
    let currentSum = 0;
    for(let fast = 0; fast < arr.length; fast++){
        currentSum += arr[fast];
        while(currentSum >= target){
            minLength = Math.min(minLength, fast - slow + 1);
            currentSum -= arr[slow];
            slow++;
        }
    }
    return minLength === Number.MAX_SAFE_INTEGER ? "No subarray exists" : minLength
}

console.log(smallestContiguousSubarray([2,1,5,2,3,2],7))
console.log(smallestContiguousSubarray([2, 1, 7, 1, 3, 2],7))
console.log(smallestContiguousSubarray([2,2,2,2,2,2],100))
console.log(smallestContiguousSubarray([2, 1, 5, 1, 3, 2,1,1,1,1,1,1,1,1,1],7))
console.log(smallestContiguousSubarray([1,1,1,1,1,1,1,1,1],4))
console.log(smallestContiguousSubarray([1,2,3,4,5,6,7,8,9],3))
console.log(smallestContiguousSubarray([2,4,3,6,5,7,8],4))
console.log(smallestContiguousSubarray([10],40))
console.log(smallestContiguousSubarray([],40))
console.log(smallestContiguousSubarray([8],7))
console.log(smallestContiguousSubarray([8],8))
console.log(smallestContiguousSubarray([8],9))

08/06/2026
Given an array of positive integers and an integer k, 
return the length of the longest contiguous subarray whose sum is less than or equal to k.
arr=[10,10,10]  k=5
arr=[3]         k=2
arr=[1,1,1,1]   k=0
function LongestSubArray(arr,k){
    let slow = 0;
    let maxLength = Number.MIN_SAFE_INTEGER;
    let currentSum = 0;
    for(let fast = 0; fast < arr.length; fast++){
        currentSum += arr[fast];
        while(currentSum > k){
            currentSum -= arr[slow];
            slow++;
        }
        maxLength = Math.max(maxLength, fast - slow + 1);
    }
    return maxLength === Number.MIN_SAFE_INTEGER || maxLength === 0 ? "No such subarray" : maxLength;
}

Longest Substring Without Repeating Characters

function longestSubstring(str){
    let slow = 0;
    let maxLength = Number.MIN_SAFE_INTEGER;
    let set = new Set();
    for(let fast = 0; fast < str.length; fast++){

        while(set.has(str[fast])){
            set.delete(str[slow]);
            slow++;
        }
        set.add(str[fast]);
        maxLength = Math.max(maxLength, fast - slow + 1);
    }
    return (maxLength === Number.MIN_SAFE_INTEGER || maxLength === 0) ? "No subarray" : maxLength
}

[2,1,5,2] k=7
[], k=8
[8], k=8
[7], k=8
[9], k=8
[1,1,1,1] k=0
Smallest Subarray With Sum ≥ Target
function smallestContiguousSubarray(arr,k){
    let slow = 0;
    let currentSum = 0;
    let minLength = Number.MAX_SAFE_INTEGER;
    for(let fast = 0 ; fast < arr.length; fast++){
        currentSum += arr[fast];
        while(currentSum >= k){
            minLength = Math.min(minLength, fast - slow + 1);
            currentSum -= arr[slow];
            slow++;
        }
    }
    return (minLength === Number.MAX_SAFE_INTEGER || minLength === 0) ? "No subarray" : minLength
}

Input:
arr = [1,2,1,0,1,1,0]
k = 4
Output:
5
Pattern: sli wind
State: currSum
Invariant: k
Condition: 
TC: O(n)
Given an array of positive integers arr and an integer k, 
return the length of the longest contiguous subarray whose sum is strictly less than k.
function sumLessThanK(arr, k){
    let slow = 0;
    let currentSum = 0;
    let maxLength = Number.MIN_SAFE_INTEGER;

    for(let fast = 0; fast < arr.length; fast++){
        currentSum += arr[fast];
        while(currentSum >= k){
            currentSum -= arr[slow];
            slow++;
        }
        maxLength = Math.max(maxLength, fast - slow + 1);
    }
    return (maxLength === Number.MIN_SAFE_INTEGER || maxLength === 0) ? 0 : maxLength
}


Input:
arr = [1,2,1]
k = 3
Output:
5
Pattern: sli wind
State: currSum, count
Invariant: k
Condition: 
TC: O(n)
Given an array of positive integers arr and an integer k, 
return the total number of contiguous subarrays whose sum is less than or equal to k.
function totalNumberContSubArray(arr,k){
    let slow = 0;
    let count = 0;
    let currentSum = 0;
    for(let fast = 0; fast < arr.length; fast++){
        currentSum += arr[fast];
        while(currentSum > k){
            currentSum -= arr[slow];
            slow++;
        }
        count = count + fast - slow +1
    }
    return count;
}










