1. 
normal binary search [array ust be sorted and lookup of element must be in o(1)]
function binarySearch(arr, target){
    let low = 0;
    let high = arr.length - 1;
    let mid;
    while(low <= high){
        mid = low + Math.floor((high-low)/2)

        if(arr[mid] === target) return mid;

        if(arr[mid] > target){
             high = mid - 1;
        }else{
             low = mid + 1;
        }
    }

    return -1;
}

2.Problem: First Occurrence in a Sorted Array

Given a sorted array with duplicates:

[1, 2, 2, 2, 3, 4, 5]

Target:

2

Return index of the first Occurrence
function firstOccurrence(arr, x) {
    let low = 0;
    let high = arr.length - 1;
    let mid;
   let ans = -1;
    while (high >= low) {
        mid = low + Math.floor((high - low) / 2);


        if (arr[mid] == x){
            ans = mid;
            high = mid-1;
          continue;
        }
        if (arr[mid] > x)
            high = mid - 1;
        else {
            low  = mid + 1;
        }
            
    }
    return ans;
} 

3.Last Occurrence


function lastOccurrence(arr, x) {
    let low = 0;
    let high = arr.length - 1;
    let mid;
   let ans = -1;
    while (high >= low) {
        mid = low + Math.floor((high - low) / 2);


        if (arr[mid] == x){
            ans = mid;
            low = mid+1;
          continue;
        }
        if (arr[mid] > x)
            high = mid - 1;
        else {
            low  = mid + 1;
        }
            
    }
    return ans;
}

4.Problem: Search Insert Position

Given a sorted array of distinct integers, return:

The index if the target exists.
Otherwise, the index where it should be inserted to keep the array sorted.

Examples:

arr = [1,3,5,6], target = 5
→ 2
arr = [1,3,5,6], target = 2
→ 1
arr = [1,3,5,6], target = 7
→ 4
arr = [1,3,5,6], target = 0
→ 0

function searchInsertPosition(arr, x) {
    let low = 0;
    let high = arr.length - 1;
    let mid;

    while (high >= low) {
        mid = low + Math.floor((high - low) / 2);

        if (arr[mid] == x){
            return mid;
        }
        if (arr[mid] > x)
            high = mid - 1;
        else {
            low  = mid + 1;
        }
    }
    // at loop termination condition is low > high so no need to write low > high ? low : high
    return  low
}

5. Problem: Lower Bound

Definition:

Return the index of the first element that is greater than or equal to x.

If no such element exists, return arr.length.

Examples:

arr = [1,2,2,3,5]
x = 2
→ 1

(first element >= 2 is at index 1)

arr = [1,2,2,3,5]
x = 4
→ 4

(first element >= 4 is 5)

function lowerBound(arr, x) {
    let low = 0;
    let high = arr.length - 1;
    let mid;
  let ans = -1;
    while (high >= low) {
        mid = low + Math.floor((high - low) / 2);
        if (arr[mid] >= x){
          ans = mid;
          high = mid - 1;
        }
        else {
            low  = mid + 1;
        }
      console.log(low,high, mid);
    }
    return  ans === -1 ? arr.length : ans
}

6.Problem: Upper Bound

Definition:

Return the index of the first element that is strictly greater than x.

If no such element exists, return arr.length.

Examples
arr = [1,2,2,3,5]
x = 2
→ 3

(first element > 2 is 3)

arr = [1,2,2,3,5]
x = 4
→ 4

(first element > 4 is 5)


function upperBound(arr,x){
  let low = 0;
  let high = arr.length - 1;
  let mid;
  let ans = -1;
  while(low<=high){
    mid = low + Math.floor((high-low)/2); 
    if(arr[mid] > x){
      ans = mid;
      high = mid -1;
    }else{
      low = mid + 1;
    }
    console.log(low, high, mid);
  }
  return ans === -1 ? arr.length : ans;
}
_______________________________________________________
| Problem          | Candidate Condition  | Direction |
| ---------------- | -------------------- | --------- |
| First Occurrence | `arr[mid] == target` | Left      |
| Last Occurrence  | `arr[mid] == target` | Right     |
| Lower Bound      | `arr[mid] >= target` | Left      |
| Upper Bound      | `arr[mid] > target`  | Left      |
|_____________________________________________________|

7.Next Problem: Search in Rotated Sorted Array

Example:

arr = [4,5,6,7,0,1,2]
target = 0

Answer:

Is left half sorted?
│
├── Yes
│   ├── Target in [low, mid)?
│   │      → Search left
│   └── Otherwise
│          → Search right
│
└── No (right half is sorted)
    ├── Target in (mid, high]?
    │      → Search right
    └── Otherwise
           → Search left

function searchInRotatedArray(arr,x){
  let low = 0;
  let high = arr.length - 1;
  let mid;
  while(low<=high){
    mid = low + Math.floor((high-low)/2); 
    if(arr[mid] == x) return mid;
    
    if(arr[low] <= arr[mid]){
       if(x >= arr[low] && x < arr[mid]){
           high = mid - 1;
       }else{
         low = mid + 1;
       }
    }
    else{
       if(x > arr[mid] && x <= arr[high]){
           low = mid + 1;
       }else{
           high = mid - 1;
       }
    }
    console.log(low,high,mid)
  }
  return -1;
}