Recursion

Write the design for:

Deep Clone - 

Base Case - obj === null || typeof obj !== 'object' return obj
newObj = Array.isArray(obj) ? [] : {};
What gets smaller - current ---> child
Recursive Relationship - newObj[key] = deepClone(obj[key])
return newObj;
Traversal Type - arbitrary object traversal
Return Style - child passes newobj to parent combines them


Generate Valid Parentheses - 

Base Case openCount === n && closeCount === n return [current]
What gets smaller openCount, closeCount
Recursive Relationship - 

    if(openCount < n) openArray = genValidP(current + '(', openCount + 1, closeCount, n);
    else openArray = [];
    if(closeCount < openCount) closeArray = genValidP(current + ')', openCount, closeCount + 1, n);
    else closeArray = [];
return [...openArray, ...closeArray]
Traversal Type - branching
Return Style - child returns valid parenthesis, parent merges them.


Write the design, not code.

Next Greater Element

Daily Temperatures

